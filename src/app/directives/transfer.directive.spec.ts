import { TransferDirective } from './transfer.directive';

describe('TransferDirective', () => {
  it('should create an instance', () => {
    const directive = new TransferDirective();
    expect(directive).toBeTruthy();
  });
});
